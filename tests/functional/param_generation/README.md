# Parameter Generation Example

This example demonstrates using `!param_sweep` to run the same task multiple times with different parameter combinations. This is a trivial example using the basic test suite at the moment.
