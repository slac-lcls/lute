# Basic REST API tests
This test exercises some of the REST API available via `maestro`.

In particular, it tests the basics of the mechanism that allows Tasks to talk with each other via Executors and maestro. (E.g. see what is running parallel, send requests to specific Tasks from that list and so on).
